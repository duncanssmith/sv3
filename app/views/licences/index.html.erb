<h3>
				Asset CIO for
    <% @clients.each do |client| %>
			<%=h client.name %>
		<% end %>
</h3>
<h1>Licences</h1>

<% if @licences.blank? %>
				<p>There are no licences listed</p>
<% else %>

<% javascript "pagination" %>

<div id="licences">
<%= render :partial => 'licences' %>
</div>
<% end %>
<br />

<%= link_to 'New licence', new_licence_path %>

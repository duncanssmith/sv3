<h1>Listing devices for
    <% @clients.each do |client| %>
			<%=h client.name %>
		<% end %>
		</h1>

<% javascript "pagination" %>

<div id="devices">
<%= render :partial => 'devices' %>
</div>

<br />

<%= link_to 'New device', new_device_path %>
